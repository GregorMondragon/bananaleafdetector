rootProject.name = "bananaleaf_detector"
include ':app'

// If you add the OpenCV Android SDK as a local module (see README.md,
// "OpenCV setup" section), uncomment and point this at the folder:
// include ':opencv'
// project(':opencv').projectDir = new File('opencv/sdk/java')
