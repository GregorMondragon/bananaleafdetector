<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="4dp"
    android:layout_marginEnd="4dp"
    android:layout_marginTop="2dp"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="18dp"
    app:cardElevation="8dp"
    app:cardMaxElevation="12dp"
    app:strokeWidth="0dp"
    android:outlineAmbientShadowColor="@color/card_shadow_ambient"
    android:outlineSpotShadowColor="@color/card_shadow_spot"
    app:cardBackgroundColor="@color/white"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Top Row: Type Tag Pill + Leaf Accent Icon -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/discoverDiseaseType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_status_pill_healthy"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:paddingTop="3.5dp"
                android:paddingBottom="3.5dp"
                android:textSize="10.5sp"
                android:textStyle="bold"
                android:letterSpacing="0.03"
                android:textColor="@color/status_healthy_text"
                tools:text="Fungal • Leaf disease" />

            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_leaf_logo"
                app:tint="@color/text_hint"
                android:alpha="0.6"
                android:contentDescription="Condition info" />

        </LinearLayout>

        <!-- Disease Name -->
        <TextView
            android:id="@+id/discoverDiseaseName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="@color/leaf_green_dark"
            android:textSize="16.5sp"
            android:textStyle="bold"
            tools:text="Black Sigatoka" />

        <!-- Overview Snippet -->
        <TextView
            android:id="@+id/discoverDiseaseOverview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:lineSpacingExtra="3.5dp"
            android:maxLines="2"
            android:ellipsize="end"
            tools:text="Black Sigatoka is a fungal leaf disease caused by Pseudocercospora fijiensis. It damages leaf tissue and can reduce photosynthesis and yield when severe." />

        <!-- Subtle Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="10dp"
            android:background="@color/divider" />

        <!-- Bottom Action Row (Matching My Garden 'View Analysis' Style) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Symptoms &amp; Treatment Guide"
                android:textColor="@color/text_hint"
                android:textSize="11.5sp" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="View Details"
                    android:textColor="@color/leaf_green"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.01" />

                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:layout_marginStart="3dp"
                    android:src="@drawable/ic_arrow_forward"
                    app:tint="@color/leaf_green"
                    android:contentDescription="View details" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
